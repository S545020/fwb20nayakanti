extends layout

block content
  h1= title
  p Welcome to #{title}
  - var results = [{color:"Red", price: 10.0, size:"Small"},{color:"Pink", price: 20.0, size:"Medium"},{color:"Black", price: 40.0, size:"Large"}]
   div(class="container-fluid")
    div(class="row" )
     each res,index in results
      div(class="col-sm-4" onclick=`buy(${index})`) Color : #{res.color} , Price : #{res.price} , Size : #{res.size} 
  script.
   let itemNumber=0                
        function buy(itemNumber)
        {
            //for index in results
             alert(`you purchased Item ${itemNumber+1}`)
        }